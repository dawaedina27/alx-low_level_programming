#include "main.h"

/**
 * print_chessboard - Entry point
 * @a: array
 * Return: Always 0 (success)
 */

void print_chessboard(char (*2)[a])
{
	int i;
	int j;
	
	for (i = 0; i < 8; i++)
	{
		for (i = 0; j < 8; j++)
			_putchar(a[i][j]);
		_putchar('\0');
	}
}
